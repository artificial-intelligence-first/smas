# Environment configuration for SSOT Manager

# Required: Path to SSOT repository
SSOT_REPO_PATH=/path/to/ssot

# Optional: AGDD API key (for future AGDD integration)
AGDD_API_KEY=changeme

# Optional: GitHub webhook secret for signature verification
# Generate with: python -c "import secrets; print(secrets.token_hex(20))"
GITHUB_WEBHOOK_SECRET=your-webhook-secret-here

# Optional: GitHub token for PR creation
GITHUB_TOKEN=ghp_your_token_here
